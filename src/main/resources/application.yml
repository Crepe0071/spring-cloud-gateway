server:
  port: 8000

spring:
  application:
    name: api-gateway

#  cloud:
#    gateway:
#      httpclient:
#        wiretap: true

logging:
  level:
    org.springframework.cloud.gateway: trace
    reactor.netty.http.server: DEBUG